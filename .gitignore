# build output
dist/
# generated types
.astro/

# NOTE: src/paraglide/ is intentionally NOT ignored. The inlang/paraglide
# compiler fetches its message-format plugin over the network on each
# build, which fails inside agent sandboxes with restricted egress. We
# commit the compiled output so `astro build` works offline. See CLAUDE.md
# for the agent-facing rule.

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables — ignore every .env variant, allow only the committed template.
# If this project is later provisioned with dotenvx (see CLAUDE.md "Secrets"), the
# ENCRYPTED .env.local/.env.production get un-ignored with `!` exceptions below — and
# .env.keys stays ignored. Until then, nothing here is committed.
.env
.env.*
!.env.example

# typescript incremental build cache
*.tsbuildinfo

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
.vercel/
